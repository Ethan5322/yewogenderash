// Test stub for the `server-only` package. Outside Next's RSC bundler there is
// no react-server condition, so the real module throws on import. Server-only
// code is still exercised in tests via this no-op.
export {};
